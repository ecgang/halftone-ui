# Implementation Plans

Generated by the improve skill on 2026-07-17 (audit at commit `8072748`).
Execute in the order below unless dependencies say otherwise. Each executor:
read the plan fully before starting, honor its STOP conditions, and update your
row when done.

> **Pre-existing document**: `halftone-kit-extraction.md` is the original
> hand-written extraction plan (P1–P5). It is **HISTORICAL — fully delivered**:
> `halftone-kit/{core,react,vue}`, Studio, and the degit README all shipped
> (see `git log` P1…P5 commits). Its status header ("not yet approved to
> build") is stale; keep the file as a design record, don't execute it.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | One `verify` command + CI on every push | P1 | M | — | DONE (387ffc2, reviewed + integrated 2026-07-17) |
| 002 | Repo-level CLAUDE.md (operational knowledge) | P1 | S | — (001 refines it) | DONE (1f0fcfa, reviewed + integrated 2026-07-17) |
| 003 | Docs-site samples teach the real API | P1 | M | — | TODO |
| 004 | Unit vectors: core math + Studio reducer | P2 | S | — (001 registers it) | TODO |
| 005 | Studio memo + draw-facade + shared adapter modules | P2 | S | — | TODO |
| 006 | Wire the `wash` dial for real (core + golden cycle) | P2 | M | — | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) |
REJECTED (with one-line rationale).

## Dependency notes

- 001 before 004 is *convenient* (004 registers its harness in `verify-all.mjs`);
  both carry the conditional step, so either order works.
- 002's drift check adapts to whether 001 landed (documents `npm run verify`
  vs the per-suite list).
- 003 and 006 both run the golden cycle but with OPPOSITE expectations:
  003 deliberately re-baselines (layout shifts washes); 006 must come back
  byte-identical. **Do not run them concurrently**, and if both execute, land
  006 first so its byte-identity gate isn't confounded by 003's re-baseline.
- 005 and 006 both touch adapter test files — sequential execution avoids
  merge noise; order between them doesn't matter otherwise.

## Fixed during the audit (no plan needed)

- **SEC-01** unbounded imported press dials reaching the Poisson allocator
  (terabyte grid / `Int32Array(Infinity)`) — fixed in-branch, commit `8072748`,
  with a hostile-import regression (verify-studio 38/38).
- **SEC-02** uncapped free-text scene props — same commit (500-char cap,
  2MB src cap).
- **Stray `a.out`** at root — `.gitignore` now covers `a.out`/`*.out`
  (file left on disk for the owner to delete).

## Findings considered and rejected

- **React 18 → 19 migration**: adapters use no 19-only APIs; real cost of
  staying is ~zero today. Revisit only when adopting Actions/`use`.
- **Landing page Studio link** (index.html has zero `/studio/` links): real but
  one-line; below plan threshold — fold into any landing-page touch.
- **Dead `makeNoise` per rebuild** (`core/press.js:100` assigns `s.noise`,
  nothing consumes it): deliberately NOT removed — plan 006's maintenance notes
  point at press-mechanics motion (ink-train drift) that would consume noise;
  remove or wire it when that decision lands. Golden-cycle cost outweighs the
  allocation win today.
- **Stale extraction-plan status** (`halftone-kit-extraction.md` says "not yet
  approved"): resolved by the historical note at the top of this index.

## Direction options (maintainer's list — not scheduled)

Recorded from the audit for future selection; each would start as a
design/spike plan:

1. **Close the docs↔kit gap**: donut/pie/radar/sparkline/heatmap as new tone
   fields over `core/charts.js` — the highest-visibility docs demos users
   can't install. (M–L; doubles the React+Vue mirror surface per component.)
2. **Productize the 4-plate press**: misregistration/CMYK exists in the engine
   (masthead, `drawPlates`) but no adapter exposes it — the signature
   differentiator is invisible above raw `press()`. (M spike.)
3. **Studio scene permalinks**: the store is already plain serializable JSON —
   URL-hash encoding is nearly free; multi-select/snap-align are NOT cheap
   (single `selectedId`, no geometry helpers). (S for hash-share.)
