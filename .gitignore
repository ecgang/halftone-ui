# Vercel's local link/build artifacts
.vercel

# Secrets, including whatever `vercel env pull` drops here. Templates stay
# trackable on purpose — they carry no secrets and onboarding needs them.
.env
.env.local
.env.*.local
